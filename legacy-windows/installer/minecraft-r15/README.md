# Archived installer driver

This directory preserves an earlier packaging driver. It is not the current
release or a current compatibility claim. For the active Minecraft 26 release,
use `../minecraft26-release` and the repository's `LEGACY_WINDOWS_BUILD.md`.
